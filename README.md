# Expense Analysis

A data analysis project that loads expense data from a CSV file and visualizes spending patterns using pandas and matplotlib.

## What it does
- Loads and summarizes expense data
- Breaks down total spending by category (bar chart)
- Tracks spending trends over time (line chart)

## Requirements
- Python 3
- pandas (pip install pandas)
- matplotlib (pip install matplotlib)

## Usage
